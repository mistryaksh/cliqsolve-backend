export interface AccountSettingProps {
     TwoFA: boolean; // Two factor authentication
     pushNotification: boolean; // User wants to get register notification
     storeCardPermission: boolean; // User wants to store card permissions
}
